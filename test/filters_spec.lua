require 'lib.components'

describe('Filters', function()
  test('filter', function ()
    -- local moveable = Filter.new("position", "velocity")
    -- local drawable = Filter.new("position", "size", "color")
    -- local starving = Filter.new("health", "hunger")
  end)
end)
